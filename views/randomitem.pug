extends layout

block content
  h1= title
  - var IMG = ["1.jpeg", "2.jpeg", "3.jpeg", "4.jpeg", "5.jpeg"]
  - for (let i = 0; i < 3; i++)
    - var random = Math.floor(Math.random()* 5)
    - var selected_image = IMG[random]
    img(src=`/images/${selected_image}`, `alt=Random Image ${i+1}`, height="300", width="300")